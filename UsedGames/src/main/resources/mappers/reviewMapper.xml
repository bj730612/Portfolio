<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTO Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.zerock.mapper.ReviewMapper">

<!-- 댓글 등록 -->
	<insert id="insertReview">
		INSERT INTO review (board_idx, user_idx, comment, image)
		VALUES (#{boardIdx}, #{userIdx}, #{comment}, #{image})
	</insert>

<!-- 댓글 리스트 -->
	<select id="selectReview" resultType="ReviewVO">
		SELECT u.name, r.comment, r.write_date
		from user u, review r
		where r.board_idx = #{boardIdx} and u.idx = r.user_idx
		order by r.write_date desc;
	</select>
	
</mapper>