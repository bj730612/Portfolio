<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTO Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.zerock.mapper.ReviewMapper">

<!-- 댓글 등록 -->
	<insert id="insertReview">
		INSERT INTO review (board_idx, user_idx, content)
		VALUES (#{boardIdx}, #{userIdx}, #{content})
	</insert>

<!-- 댓글 리스트 -->
	<select id="selectReview" resultType="ReviewVO">
		SELECT a.name, b.content, b.write_date
		FROM member a, review b
		WHERE b.board_idx = #{boardIdx} AND a.idx = b.user_idx
		ORDER BY b.write_date DESC;
	</select>
	
</mapper>